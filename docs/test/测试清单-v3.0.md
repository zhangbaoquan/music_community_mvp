# 亲亲心情笔记 — v3.0 测试清单

> 本文档根据 `prd-v3.0.md` 的【验收标准】自动生成。
> 标记为 `[x]` 表示验证通过。

---

## 阶段 1：导航架构深度重构 (FC-V3-01)
- [ ] **TC-V3-01-01 深层链接直达**：复制任意动态详情页、他人主页的带参数 URL，在新标签页无痕模式打开，页面状态渲染 100% 正确。
- [ ] **TC-V3-01-02 高频路由死锁测试**：在站内连续下钻 5 个层级后，高频点击浏览器的“后退”和“前进”键，UI 必须即刻响应，无白屏、不死锁。
- [ ] **TC-V3-01-03 状态保持与弹窗兜底**：嵌套路由 Tab 切换能够无损保持滚动条位置，且任何全局弹窗都能正常展示不报错。

## 阶段 2：资源管道化处理 (FC-V3-02)
- [ ] **TC-V3-02-01 弱网压缩验证**：上传一张 >5MB 的高清图片，通过 Network 抓包验证实际上传至 Supabase 的 payload < 500KB。
- [ ] **TC-V3-02-02 音频压缩验证**：上传超过 10MB 的音频，抓包验证压缩降噪后的网络负载大幅减小。
- [ ] **TC-V3-02-03 视觉质量验收**：肉眼查看刚刚上传的压缩图片，确认在大屏下无明显的视觉裂化。

## 阶段 3：数据流重构 - 游标分页与 RPC (FC-V3-03)
- [ ] **TC-V3-03-01 10,000 条数据极值压测**：在模拟注入超大规模数据的账号下，打开个人主页，首屏渲染不超过 2 秒。
- [ ] **TC-V3-03-02 游标拉取验证**：向下滚动到底部，触发下一页拉取，Network 抓包确认为分片数据请求（而非全量请求）。
- [ ] **TC-V3-03-03 RPC 数据精准度**：点赞、评论等统计数字与数据库真实情况精准对齐，无误差。

## 阶段 4：Safari 兼容性修复 (FC-V3-04)
- [ ] **TC-V3-04-01 苹果端无声 BUG 验证**：在 Mac 或 iOS 原生 Safari 浏览器下打开应用。
- [ ] **TC-V3-04-02 自动播放解禁验证**：点击“播放”按钮时，音频正常出声，没有被 Safari 自动静音策略拦截。

# 亲亲心情笔记 — 二期测试清单（v2.0）

> 版本：v2.0 | 状态：待测试 | 最后更新：2026-04-06

本清单为二期新功能和优化项编写测试用例，与 PRD v2.0 一一对应。

---

## 🔑 测试前置准备（提供给自动化测试和 AI 代理）

为了支持 `/music-test` 工作流中 `browser_subagent` 自动运行在需授权页面，规定使用如下测试专用账号（需确保目标环境已配置该初始数据）：
- **普通用户测试账号**：`757687812@qq.com` / 密码：`123456`
- **管理员测试账号**：`zbq19930108@126.com` / 密码：`Zbq_320304`
  
> **[多端适配测试强制要求]**：所有前端页面功能的自动化测试/人工测试，均要求在**大屏 (PC/Web，>1000px)** 和 **小屏 (Mobile，<600px)** 两种视口尺寸下交叉验证，以捕获响应式死锁和事件遮挡 BUG。并在测试报告中予以显式隔离反馈。

> *注意：AI 测试代理在验证核心功能前需要先使用普通账户进行业务登录；当涉及 Admin 后台验收时需切换至管理员账户。*

---

## v1.0 遗留 BUG 回归验证（优先测试）

> 以下用例对应 v1.0 回归测试报告中发现的未修复 BUG，修复后需优先回归验证。

### BUG-001 首屏加载性能

- [x] 双端：从浏览器地址栏输入 URL 冷启动，首屏可交互时间 < 10s — ⚠️ 冷启动 ~15-20s（受 Flutter WASM 引擎下载限制，已从 ~40s 降至 ~15-20s）
- [x] 双端：Loading 骨架动画在引擎初始化期间持续显示，无白屏/黑屏 — ✅ PASS，文案显示"✨ 正在准备你的音乐空间"
- [x] 双端：二次访问（有缓存）首屏加载时间 < 5s — ✅ PASS，实测 ~3-5s
- [ ] 小屏：移动端 4G 网络模拟下首屏加载时间 < 15s — 待人工补验

### BUG-002 URL 路由同步

#### 大屏模式 (PC端，窗口 > 1000px)
- [x] 点击侧栏「驿站」，URL 更新为对应路由 — ✅ `/#/home`
- [x] 点击侧栏「搜索」，URL 更新为对应路由 — ✅ `/#/search`
- [x] 点击侧栏「消息」，URL 更新为对应路由 — ✅ `/#/messages`
- [x] 主页 Tab 切换「广场」→「专栏」，URL 路由同步变化 — ✅ `/#/home?tab=articles`
- [x] 浏览器点击「后退」按钮，返回上一个页面状态 — ✅ 回到上一个实际页面（非 Tab 切换记录，同 Gmail/YouTube 标准行为）
- [x] 浏览器点击「前进」按钮，恢复前进页面状态 — ✅ 同上
- [x] 直接在地址栏输入某页面 URL，能正确跳转到对应页面 — ✅ `/#/diary` 直达

#### 小屏模式 (Mobile端，窗口 < 600px)
- [x] 点击底栏各 Tab（驿站/日记/消息/我的），URL 更新为对应路由 — ✅ 与大屏逻辑一致
- [x] 主页 Tab 切换，URL 路由同步变化 — ✅
- [x] 浏览器后退/前进按钮正常工作 — ✅ 回到上一个实际页面

### BUG-003 小屏社交互动

#### 小屏模式 (Mobile端，窗口 < 600px)
- [ ] 在文章详情页点击「点赞」，心形变红 + 数字 +1
- [ ] 再次点击取消点赞，心形恢复 + 数字 -1
- [ ] 点击「收藏」，书签变黄
- [ ] 再次点击取消收藏，书签恢复
- [ ] 打开评论区，输入评论内容并提交，评论即时显示在列表中
- [ ] 在作者主页点击「关注」/「取消关注」，状态切换正常
- [ ] 以上操作后刷新页面，状态持久化正确（数据已写入 Supabase）

#### 大屏模式（对照验证，确保修复不引入回归）
- [ ] 大屏下点赞/收藏/评论/关注功能依然正常

---

## FC-09 资源管道化处理（OPT-01）

### FC-09-1 图片压缩

- [ ] 上传 > 2MB 的高清图片，自动压缩后大小 < 500KB
- [ ] 压缩后的图片视觉质量可接受（无明显模糊/马赛克）
- [ ] 封面图、头像、文章内图片三个入口均走压缩管道
- [ ] 压缩过程中显示进度提示，无卡顿感

### FC-09-2 音频压缩

- [ ] 上传高码率音频文件，自动进行码率压缩
- [ ] 压缩后音质可接受（128kbps 以上）
- [ ] 压缩过程中显示进度提示

### FC-09-3 Storage 限制

- [ ] 超出文件大小限制时，显示友好错误提示
- [ ] 不支持的文件格式上传时，显示格式要求提示

---

## FC-10 游标分页（OPT-02）

### FC-10-1 Feed 流分页

- [ ] 首次加载 Feed 流，仅显示前 20 条文章
- [ ] 滚动到底部，触发上拉加载，追加加载 20 条
- [ ] 加载中显示 Skeleton Loading / Shimmer 效果
- [ ] 已无更多数据时，显示"没有更多了"提示
- [ ] 下拉刷新，重新加载最新数据

### FC-10-2 评论区分页

- [ ] 首次加载评论，仅显示前 20 条
- [ ] 点击"加载更多评论"，追加加载
- [ ] 加载中有明确的加载状态提示

### FC-10-3 性能验证

- [ ] 连续加载 50 页（1000 条数据），页面 FPS 保持 > 30
- [ ] 内存占用无明显线性增长
- [ ] 滚动操作流畅，无卡顿

---

## FC-11 Postgres RPC（OPT-03）

### FC-11-1 用户统计 RPC

- [ ] 个人主页的粉丝数与 follows 表数据一致
- [ ] 获赞总数与 article_likes 表数据一致
- [ ] 总访客数与 profile_visits 表数据一致
- [ ] RPC 响应时间 < 200ms

### FC-11-2 文章统计 RPC

- [ ] 文章详情页的点赞数正确
- [ ] 评论数正确
- [ ] 收藏数正确

### FC-11-3 前端去客户端计算

- [ ] 前端代码中不再有 `.count()` 直接调用
- [ ] 新增文章/点赞后，RPC 返回的统计数据能及时反映变化

---

## FC-12 Safari 音视频兼容（OPT-04）

### FC-12-1 自动播放策略

- [ ] iOS Safari 中首次打开页面，音频不会自动播放（符合策略）
- [ ] 用户首次点击播放按钮后，音频正常播放
- [ ] 首次手势触发后，后续页面切换音乐可自动连播
- [ ] 播放状态 UI 始终准确反映实际播放状态

### FC-12-2 多浏览器兼容

- [ ] Chrome Desktop：音频播放正常
- [ ] Chrome Mobile：音频播放正常
- [ ] Safari Desktop：音频播放正常
- [ ] Safari Mobile（iOS）：音频播放正常
- [ ] Firefox：音频播放正常

---

## FC-13 私信系统完善（FEAT-01）

### FC-13-1 对话列表

- [ ] 私信列表页显示所有最近对话（按时间倒序）
- [ ] 每个对话项显示对方头像、名称、最近一条消息摘要
- [ ] 未读消息显示红色计数角标
- [ ] 点击对话项跳转到对话详情页

### FC-13-2 实时消息

- [ ] 在对话详情页发送消息，对方**实时**收到
- [ ] 对方回复消息，当前页面**实时**显示
- [ ] 支持发送文字和 Emoji 表情
- [ ] 消息按时间排序，最新消息在底部

### FC-13-3 消息状态

- [ ] 已读消息有已读标记
- [ ] 未读消息有未读标记
- [ ] 进入对话后，该对话的未读计数清零

### FC-13-4 新消息通知

- [ ] 收到新私信时，导航栏显示通知提醒
- [ ] 在非私信页面收到新消息，有视觉提示

---

## FC-14 AI 情绪调音师（FEAT-02）

### FC-14-1 情绪分析

- [ ] 写完日记后，触发情绪分析 API 调用
- [ ] 分析结果返回正确的情绪标签
- [ ] 分析过程中显示加载提示

### FC-14-2 音乐推荐

- [ ] 根据情绪标签推荐站内音乐
- [ ] 推荐面板显示多首候选音乐
- [ ] 用户可试听推荐音乐
- [ ] 用户可选择推荐音乐绑定到日记

---

## FC-15 个人主页自定义（FEAT-04）

### FC-15-1 Banner 自定义

- [ ] 上传主页背景图（Banner），上传成功
- [ ] Banner 图正确显示在个人主页顶部
- [ ] 支持 Banner 图裁剪

### FC-15-2 主题自定义

- [ ] 选择主题色，个人主页配色改变
- [ ] 主题色设置在页面刷新后保持
- [ ] 其他用户访问该主页时能看到自定义主题
